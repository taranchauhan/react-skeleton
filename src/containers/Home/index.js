import React from 'react';
import styled from 'styled-components';

const StyledDiv = styled.div`
  margin-top: 10px;
`;

const Home = () => (
  <StyledDiv>
    <h2>Home</h2>
  </StyledDiv>
);

export default Home;
