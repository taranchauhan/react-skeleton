import React from 'react';
import styled from '@emotion/styled';

const StyledDiv = styled.div`
  margin-top: 10px;
`;

const Home = () => (
  <StyledDiv>
    <h2>Home</h2>
  </StyledDiv>
);

export default Home;
